# LedgerFlow

A double-entry bookkeeping engine built the way real payment ledgers work: every transaction
is a balanced set of debits and credits, enforced in code before anything is written to the
database.

> 🚧 Under active development. Full architecture, setup instructions, and API walkthrough
> land in later commits — see the commit history for build progress.
